# @mnemo/shared

Canonical TypeScript types for the web app and the Node sidecar. Python
services have their own Pydantic equivalents — when you change something
here, change the Python models too. For the hackathon we sync by hand.
